# 📁 هيكل ملفات المشروع - Project Structure

<div dir="rtl">

## 📂 شجرة الملفات الكاملة

```
telegram-numbers-bot/
│
├── 📄 bot.py                    # الملف الرئيسي للبوت ⭐
├── 📄 config.py                 # ملف الإعدادات والمتغيرات ⚙️
├── 📄 admin_handler.py          # معالج لوحة تحكم المطور 👑
├── 📄 requirements.txt          # المكتبات المطلوبة 📦
│
├── 📄 README.md                 # دليل المستخدم الشامل 📖
├── 📄 QUICKSTART.md             # دليل البدء السريع 🚀
├── 📄 LICENSE                   # رخصة المشروع 📜
│
├── 📄 .env.example              # مثال للمتغيرات البيئية 🔐
├── 📄 .gitignore                # ملفات Git المتجاهلة 🚫
│
├── 🔧 start.sh                  # سكريبت تشغيل البوت ▶️
├── 🔧 install.sh                # سكريبت التنصيب التلقائي 📥
│
└── 📂 data/                     # مجلد البيانات (يُنشأ تلقائياً) 💾
    ├── users.json              # بيانات المستخدمين
    ├── purchases.json          # سجل المشتريات
    ├── reservations.json       # الحجوزات التلقائية
    └── settings.json           # إعدادات البوت
```

---

## 📝 شرح كل ملف

### 1️⃣ `bot.py` - الملف الرئيسي

**الوصف:** قلب البوت، يحتوي على:
- نظام المستخدمين والنقاط
- معالجة الأوامر والأزرار
- منطق شراء الأرقام
- التحقق من الاشتراك
- نظام الحجز التلقائي

**الحجم:** ~21 KB
**اللغة:** Python 3.9+

**الاستخدام:**
```bash
python3 bot.py
```

---

### 2️⃣ `config.py` - ملف الإعدادات

**الوصف:** جميع إعدادات البوت القابلة للتخصيص:
- توكن البوت وبيانات الاتصال
- أسعار الدول (50+ دولة)
- الخدمات المدعومة (16+ خدمة)
- رسائل البوت
- إعدادات API
- نظام المكافآت

**الحجم:** ~15 KB
**الملفات المطلوبة:** لا يوجد

**أهم الإعدادات التي يجب تعديلها:**
```python
BOT_TOKEN = "..."          # ⚠️ مطلوب
ADMIN_IDS = [123456789]    # ⚠️ مطلوب
CHANNEL_USERNAME = "..."   # ⚠️ مطلوب
PURCHASES_CHANNEL = "..."  # ⚠️ مطلوب
```

---

### 3️⃣ `admin_handler.py` - لوحة المطور

**الوصف:** وحدة متقدمة لإدارة البوت:
- إضافة وخصم النقاط
- تعديل الإعدادات
- عرض الإحصائيات المفصلة
- إدارة المستخدمين
- الرسائل الجماعية

**الحجم:** ~19 KB
**الملفات المطلوبة:** `bot.py`, `config.py`

**المميزات:**
- ✅ نظام محادثة تفاعلي
- ✅ تأكيد قبل العمليات الحساسة
- ✅ إشعارات للمستخدمين
- ✅ تقارير شاملة

---

### 4️⃣ `requirements.txt` - المكتبات

**الوصف:** قائمة بجميع مكتبات Python المطلوبة

**المكتبات الرئيسية:**
```
python-telegram-bot==21.7    # مكتبة تليجرام
aiohttp==3.11.11            # للطلبات HTTP
cryptography==44.0.0        # للتشفير
qrcode==8.0                 # لإنشاء QR codes
```

**التثبيت:**
```bash
pip3 install -r requirements.txt
```

---

### 5️⃣ `README.md` - الدليل الشامل

**الوصف:** توثيق كامل للمشروع
**الحجم:** ~12 KB

**المحتويات:**
- 📖 نظرة عامة والمميزات
- 🚀 دليل التنصيب المفصل
- ⚙️ شرح الإعدادات
- 🎮 طريقة الاستخدام
- 🔧 حل المشاكل
- 📊 قاعدة البيانات
- 🔐 نصائح الأمان

---

### 6️⃣ `QUICKSTART.md` - البدء السريع

**الوصف:** دليل سريع للبدء في دقائق
**الحجم:** ~7 KB

**المحتويات:**
- ⚡ تنصيب في 3 خطوات
- 📱 أمثلة الاستخدام
- 🎯 حالات شائعة
- 💡 نصائح مهمة

---

### 7️⃣ `start.sh` - سكريبت التشغيل

**الوصف:** سكريبت Bash لتشغيل البوت بسهولة
**الحجم:** ~3 KB

**المميزات:**
- ✅ فحص Python و pip
- ✅ إنشاء المجلدات تلقائياً
- ✅ التحقق من المكتبات
- ✅ فحص ملف الإعدادات
- ✅ عرض رسائل واضحة

**الاستخدام:**
```bash
chmod +x start.sh
./start.sh
```

---

### 8️⃣ `install.sh` - سكريبت التنصيب

**الوصف:** معالج تنصيب تفاعلي
**الحجم:** ~5 KB

**يقوم بـ:**
- 📥 تحديث النظام
- 🔧 تثبيت المتطلبات
- ⚙️ إعداد البوت بالكامل
- 📝 طلب البيانات من المستخدم
- ✅ تطبيق الإعدادات تلقائياً

**الاستخدام:**
```bash
sudo chmod +x install.sh
sudo ./install.sh
```

---

### 9️⃣ `.env.example` - مثال المتغيرات

**الوصف:** قالب لملف المتغيرات البيئية
**الحجم:** ~4 KB

**كيفية الاستخدام:**
```bash
cp .env.example .env
nano .env  # عدّل القيم
```

**المتغيرات:**
- 🤖 إعدادات البوت
- 👑 المطورين
- 📢 القنوات
- 🔗 API
- 💳 الدفع
- 🔐 الأمان

---

### 🔟 `.gitignore` - ملفات Git

**الوصف:** قائمة الملفات التي يجب تجاهلها في Git

**يحمي:**
- 🔐 التوكنات والمفاتيح
- 💾 بيانات المستخدمين
- 📝 السجلات
- 🗂️ ملفات النظام

**الأهمية:** عالية جداً ⚠️

---

## 📂 مجلد `data/` - البيانات

### الملفات التي يتم إنشاؤها تلقائياً:

#### `users.json` - بيانات المستخدمين
```json
{
  "123456789": {
    "user_id": 123456789,
    "username": "user123",
    "first_name": "محمد",
    "points": 100,
    "purchases": 5,
    "join_date": "2024-01-01T10:00:00"
  }
}
```

#### `purchases.json` - المشتريات
```json
{
  "123456789": [
    {
      "number": "+9647700000000",
      "service": "telegram",
      "country": "iraq",
      "price": 50,
      "date": "2024-01-01T12:00:00",
      "code_channel": "CODE_123_456"
    }
  ]
}
```

#### `settings.json` - الإعدادات
```json
{
  "welcome_message": "مرحباً...",
  "force_channel": "@YourChannel",
  "enabled": true
}
```

#### `reservations.json` - الحجوزات
```json
{
  "123456789": [
    {
      "service": "whatsapp",
      "country": "saudi",
      "created_at": "2024-01-01T10:00:00",
      "status": "pending"
    }
  ]
}
```

---

## 🔄 دورة حياة الملفات

### عند التشغيل الأول:

```
1. تشغيل bot.py
   ↓
2. قراءة config.py
   ↓
3. إنشاء مجلد data/
   ↓
4. إنشاء ملفات JSON فارغة
   ↓
5. بدء البوت ✅
```

### عند استخدام البوت:

```
مستخدم يرسل /start
   ↓
حفظ بياناته في users.json
   ↓
يشتري رقم
   ↓
حفظ الشراء في purchases.json
   ↓
إرسال إشعار لقناة المشتريات
```

---

## 💾 النسخ الاحتياطي

### ملفات مهمة يجب نسخها:

1. **`data/`** - جميع البيانات ⚠️⚠️⚠️
2. **`config.py`** - الإعدادات المخصصة
3. **`.env`** - المتغيرات (إذا استخدمته)

### أوامر النسخ الاحتياطي:

```bash
# نسخ يومي
cp -r data/ backup/data_$(date +%Y%m%d)/

# نسخ كامل للمشروع
tar -czf bot_backup_$(date +%Y%m%d).tar.gz \
    bot.py config.py data/ admin_handler.py
```

---

## 📊 حجم الملفات الإجمالي

| الملف/المجلد | الحجم التقريبي |
|--------------|----------------|
| bot.py | 21 KB |
| config.py | 15 KB |
| admin_handler.py | 19 KB |
| README.md | 12 KB |
| QUICKSTART.md | 7 KB |
| requirements.txt | 1 KB |
| start.sh | 3 KB |
| install.sh | 5 KB |
| .env.example | 4 KB |
| .gitignore | 3 KB |
| LICENSE | 3 KB |
| **الإجمالي** | **~93 KB** |

**ملاحظة:** مجلد `data/` سيزداد حجمه مع الاستخدام.

---

## 🔍 الملفات المطلوبة للتشغيل

### الحد الأدنى (ملفات أساسية):

- ✅ `bot.py` - مطلوب
- ✅ `config.py` - مطلوب
- ✅ `requirements.txt` - مطلوب
- ✅ مجلد `data/` - يُنشأ تلقائياً

### ملفات اختيارية (لكن مفيدة):

- 📖 `README.md` - للتوثيق
- 🚀 `QUICKSTART.md` - دليل سريع
- ⚙️ `admin_handler.py` - لوحة المطور المتقدمة
- 🔧 `start.sh` - لتشغيل سهل
- 📥 `install.sh` - للتنصيب التلقائي

---

## 🎯 خريطة الاعتماديات

```
bot.py
  ├── config.py (مطلوب)
  ├── admin_handler.py (اختياري)
  └── data/*.json (يُنشأ تلقائياً)

admin_handler.py
  ├── bot.py
  └── config.py

start.sh
  ├── bot.py
  ├── config.py
  └── requirements.txt

install.sh
  ├── requirements.txt
  └── config.py
```

---

## 📌 ملاحظات مهمة

### ⚠️ لا تحذف هذه الملفات:

- ❌ `bot.py`
- ❌ `config.py`
- ❌ مجلد `data/` (كل البيانات هنا!)
- ❌ `requirements.txt`

### ✅ يمكن حذفها بأمان:

- ✅ `README.md` (بعد قراءته)
- ✅ `QUICKSTART.md`
- ✅ `.env.example`
- ✅ `LICENSE`

### 🔐 احمِ هذه الملفات:

- 🔒 `config.py` - يحتوي على التوكن!
- 🔒 `.env` - إذا استخدمته
- 🔒 `data/` - جميع بيانات المستخدمين

---

## 📚 ملفات إضافية يمكن إضافتها مستقبلاً:

- `api_handler.py` - لإدارة API الموقع
- `database.py` - للانتقال إلى SQL
- `payments.py` - لنظام الدفع
- `utils.py` - دوال مساعدة
- `logger.py` - نظام سجلات متقدم
- `tests/` - اختبارات تلقائية
- `docs/` - توثيق إضافي

---

<div align="center">

**✨ هيكل منظم = مشروع ناجح ✨**

لأي استفسار، راجع [README.md](README.md)

</div>

</div>
