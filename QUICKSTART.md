# 🚀 دليل البدء السريع - Quick Start Guide

<div dir="rtl">

## ⚡ التنصيب في 3 خطوات

### 1️⃣ تحميل وتثبيت

```bash
# تثبيت المكتبات
pip3 install -r requirements.txt

# أو استخدم سكريبت التنصيب التلقائي
sudo chmod +x install.sh
sudo ./install.sh
```

### 2️⃣ إعداد البوت

افتح ملف `config.py` وعدّل:

```python
BOT_TOKEN = "123456789:ABCdefGHI..."  # من @BotFather
BOT_USERNAME = "@YourBotName"
ADMIN_IDS = [123456789]  # آيديك
CHANNEL_USERNAME = "@YourChannel"
PURCHASES_CHANNEL = "@PurchasesChannel"
SUPPORT_USERNAME = "@YourSupport"
```

### 3️⃣ تشغيل البوت

```bash
python3 bot.py

# أو
./start.sh
```

---

## 📱 أمثلة الاستخدام

### للمستخدمين العاديين:

#### شراء رقم تليجرام عراقي:

1. `/start` - فتح البوت
2. اضغط "📱 التليجرام"
3. اختر "🇮🇶 العراق"
4. اضغط "✅ تأكيد الشراء"
5. استلم الرقم والكود

#### شراء رقم واتساب سعودي:

1. `/start`
2. "🛒 شراء رقم"
3. "🇸🇦 السعودية"
4. اختر "💬 واتساب"
5. تأكيد

#### معرفة الرصيد:

1. `/start`
2. اضغط "📊 حسابي"

---

### للمطورين:

#### إضافة 100 نقطة للمستخدم:

```
/start
→ ⚙️ لوحة المطور
→ ➕ إضافة نقاط
→ أرسل آيدي المستخدم: 123456789
→ أرسل عدد النقاط: 100
→ ✅ نعم، أضف النقاط
```

#### خصم 50 نقطة:

```
/start
→ ⚙️ لوحة المطور
→ ➖ خصم نقاط
→ أرسل الآيدي: 123456789
→ أرسل العدد: 50
→ ✅ تأكيد
```

#### عرض الإحصائيات:

```
/start
→ ⚙️ لوحة المطور
→ 📊 الإحصائيات
```

#### تغيير رسالة الترحيب:

```
/start
→ ⚙️ لوحة المطور
→ ✏️ تغيير الترحيب
→ أرسل الرسالة الجديدة
```

---

## 🎯 حالات الاستخدام الشائعة

### كيف أضيف دولة جديدة؟

في `config.py`:

```python
# 1. أضف الدولة للقائمة
COUNTRIES = {
    "morocco": {"name": "المغرب", "flag": "🇲🇦", "code": "+212"}
}

# 2. حدد السعر
PRICES["morocco"] = 55  # 55 نقطة
```

### كيف أضيف خدمة جديدة؟

```python
SERVICES = {
    "snapchat": "👻 سناب شات"
}
```

### كيف أغير سعر دولة؟

```python
PRICES = {
    "iraq": 40,  # كان 50، أصبح 40
}
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: البوت لا يرد

**الحل:**

```bash
# تحقق من التوكن
python3 -c "from config import BOT_TOKEN; print(BOT_TOKEN)"

# شغّل البوت بوضع Debug
python3 bot.py
```

### المشكلة: خطأ في الاشتراك الإجباري

**الحل:**

1. تأكد أن البوت **مسؤول** في القناة
2. تأكد من يوزر القناة في `config.py`
3. جرّب تعطيل الاشتراك مؤقتاً:

```python
# في config.py
MAINTENANCE_MODE = True  # أو
# في settings.json
{"enabled": false}
```

### المشكلة: لا تصل إشعارات المشتريات

**الحل:**

1. اجعل البوت مسؤول في قناة المشتريات
2. تحقق من `PURCHASES_CHANNEL` في `config.py`

---

## 💡 نصائح مهمة

### 🔐 الأمان:

```bash
# لا تشارك ملف config.py أبداً
# احفظ نسخة احتياطية:
cp -r data/ backup_$(date +%Y%m%d)/
```

### ⚡ الأداء:

```bash
# للتشغيل المستمر، استخدم screen:
screen -S telegram-bot
python3 bot.py
# اضغط Ctrl+A ثم D

# للرجوع:
screen -r telegram-bot
```

### 📊 المراقبة:

```bash
# راقب سجلات البوت:
tail -f logs/bot.log

# راقب استخدام الموارد:
htop
```

---

## 🎨 تخصيص الواجهة

### تغيير الإيموجي والنصوص:

في `config.py`، كل شيء قابل للتخصيص:

```python
WELCOME_MESSAGE = """
🌟 مرحباً {name}!

رسالتك المخصصة هنا...
"""

SERVICES = {
    "telegram": "📱 تليجرام الخاص بي",  # غيّر النص
    "myapp": "🎯 تطبيقي الجديد"  # أضف خدمة
}
```

---

## 📞 الحصول على المساعدة

### أسئلة شائعة:

**س: كيف أحصل على توكن البوت؟**
ج: من [@BotFather](https://t.me/BotFather) - أرسل `/newbot`

**س: كيف أعرف آيدي؟**
ج: من [@userinfobot](https://t.me/userinfobot)

**س: هل يمكن استخدام قاعدة بيانات SQL؟**
ج: نعم! يمكنك تطوير البوت ليستخدم PostgreSQL أو MySQL

**س: هل يدعم البوت الدفع الإلكتروني؟**
ج: حالياً يدعم النقاط. يمكنك إضافة Stripe أو PayPal لاحقاً

---

## 🚀 التشغيل على VPS

### Ubuntu/Debian:

```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت المتطلبات
sudo apt install python3 python3-pip git screen -y

# تحميل البوت
git clone https://github.com/yourusername/telegram-bot.git
cd telegram-bot

# التنصيب
sudo ./install.sh

# التشغيل في الخلفية
screen -S bot
./start.sh
# اضغط Ctrl+A ثم D
```

### CentOS/RHEL:

```bash
sudo yum install python3 python3-pip git screen -y
# ثم نفس الخطوات السابقة
```

---

## 📈 التطوير المستقبلي

### أفكار للتطوير:

- [ ] إضافة قاعدة بيانات PostgreSQL
- [ ] نظام الإحالة المتقدم
- [ ] طرق دفع متعددة
- [ ] لوحة تحكم ويب
- [ ] تطبيق موبايل مرافق
- [ ] تقارير Excel تلقائية
- [ ] إشعارات البريد الإلكتروني
- [ ] دعم لغات متعددة

---

## 🎓 موارد تعليمية

### تعلم المزيد:

- [توثيق python-telegram-bot](https://docs.python-telegram-bot.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [دورة Python](https://www.youtube.com/results?search_query=python+arabic)
- [دورة Telegram Bots](https://www.youtube.com/results?search_query=telegram+bot+tutorial)

---

## ✅ قائمة التحقق قبل الإطلاق

- [ ] توكن البوت صحيح
- [ ] آيدي المطور صحيح
- [ ] القنوات جاهزة والبوت مسؤول فيها
- [ ] الأسعار معدّلة
- [ ] رسالة الترحيب مخصصة
- [ ] تم اختبار جميع الأزرار
- [ ] نظام النقاط يعمل بشكل صحيح
- [ ] قناة المشتريات تستقبل الإشعارات
- [ ] تم إنشاء نسخة احتياطية

---

<div align="center">

**🎉 الآن أنت جاهز! وفقك الله**

إذا واجهت أي مشكلة، راجع [README.md](README.md)

</div>

</div>
