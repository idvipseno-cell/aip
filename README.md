# 🤖 بوت بيع الأرقام الافتراضية - Telegram Bot

<div dir="rtl">

## 📋 نظرة عامة

بوت تليجرام احترافي ومتكامل لبيع الأرقام الافتراضية لجميع التطبيقات والمواقع من جميع دول العالم.

### ✨ المميزات الرئيسية

- 🌍 **دعم جميع الدول** - أكثر من 50 دولة متاحة
- 📱 **دعم جميع التطبيقات** - تليجرام، واتساب، فيسبوك، انستجرام، وأكثر
- 💰 **نظام نقاط متكامل** - إدارة رصيد المستخدمين
- 🔄 **حجز تلقائي** - احجز رقم وسيُشترى تلقائياً عند توفره
- 📊 **لوحة تحكم للمطور** - إدارة كاملة للبوت
- 🎁 **نظام مكافآت** - مكافآت التسجيل والإحالة
- 📢 **قناة المشتريات** - إشعارات فورية بكل عملية شراء
- 🛡️ **نظام حماية** - منع الوهميين والاشتراك الإجباري
- 🎨 **أزرار شفافة** - واجهة مستخدم احترافية
- ⚡ **سريع وموثوق** - أداء عالي واستجابة فورية

---

## 🚀 طريقة التنصيب السريعة

### 1️⃣ المتطلبات الأساسية

تأكد من تثبيت:
- **Python 3.9+** أو أحدث
- **pip** (مدير حزم Python)
- حساب تليجرام وبوت من [@BotFather](https://t.me/BotFather)

### 2️⃣ تحميل الملفات

```bash
# استنسخ المشروع أو حمّل الملفات
git clone https://github.com/yourusername/telegram-numbers-bot.git
cd telegram-numbers-bot
```

### 3️⃣ تثبيت المكتبات

```bash
# تثبيت جميع المتطلبات
pip install -r requirements.txt
```

أو:

```bash
pip3 install -r requirements.txt
```

### 4️⃣ إعداد ملف الإعدادات

افتح ملف `config.py` وعدّل القيم التالية:

```python
# توكن البوت من BotFather
BOT_TOKEN = "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"  # ضع توكن بوتك

# يوزرنيم البوت
BOT_USERNAME = "@YourBotUsername"  # يوزر البوت

# أيدي المطورين
ADMIN_IDS = [123456789]  # ضع آيديك هنا

# قناة الاشتراك الإجباري
CHANNEL_USERNAME = "@YourChannel"  # قناتك

# قناة المشتريات
PURCHASES_CHANNEL = "@PurchasesChannel"  # قناة الإشعارات

# يوزر الدعم
SUPPORT_USERNAME = "@YourSupport"  # حساب الدعم
```

### 5️⃣ تشغيل البوت

```bash
python bot.py
```

أو:

```bash
python3 bot.py
```

### ✅ جاهز!

البوت الآن يعمل! افتح البوت في تليجرام واستخدم `/start`

---

## 📁 هيكل الملفات

```
telegram-numbers-bot/
│
├── bot.py                  # الملف الرئيسي للبوت
├── config.py               # ملف الإعدادات والمتغيرات
├── requirements.txt        # المكتبات المطلوبة
├── README.md              # هذا الملف
│
├── data/                   # مجلد البيانات (يُنشأ تلقائياً)
│   ├── users.json         # بيانات المستخدمين
│   ├── purchases.json     # سجل المشتريات
│   ├── reservations.json  # الحجوزات التلقائية
│   └── settings.json      # إعدادات البوت
│
└── logs/                   # سجلات البوت (اختياري)
    └── bot.log
```

---

## ⚙️ الإعدادات المتقدمة

### 🔑 الحصول على توكن البوت

1. افتح [@BotFather](https://t.me/BotFather)
2. أرسل `/newbot`
3. اختر اسم للبوت
4. اختر يوزرنيم للبوت (يجب أن ينتهي بـ `bot`)
5. انسخ التوكن وضعه في `config.py`

### 🆔 معرفة آيدي تليجرام

1. افتح [@userinfobot](https://t.me/userinfobot)
2. أرسل `/start`
3. انسخ الآيدي وضعه في `ADMIN_IDS`

### 📢 إنشاء قناة الاشتراك الإجباري

1. أنشئ قناة جديدة
2. اجعل البوت مسؤول في القناة
3. ضع يوزر القناة في `CHANNEL_USERNAME`

### 💰 تعديل الأسعار

في ملف `config.py`، عدّل قاموس `PRICES`:

```python
PRICES = {
    "iraq": 50,      # العراق - 50 نقطة
    "saudi": 70,     # السعودية - 70 نقطة
    "uae": 80,       # الإمارات - 80 نقطة
    # أضف المزيد...
}
```

### 🔗 ربط البوت بموقع بيع الأرقام

في `config.py`، ضع بيانات API الموقع:

```python
API_URL = "https://api.yourservice.com"
API_KEY = "your_api_key_here"
```

ثم عدّل دوال API في `bot.py` حسب توثيق موقعك.

---

## 🎮 طريقة الاستخدام

### للمستخدمين:

1. **البدء**: `/start` - بدء البوت
2. **شراء رقم**: اضغط "🛒 شراء رقم"
3. **اختيار الدولة**: اختر الدولة من القائمة
4. **اختيار الخدمة**: اختر التطبيق (تليجرام، واتساب، إلخ)
5. **تأكيد الشراء**: اضغط "✅ تأكيد"
6. **استلام الرقم**: ستحصل على الرقم والكود

### للمطورين:

#### 1️⃣ إضافة نقاط للمستخدم

```
/start → ⚙️ لوحة المطور → ➕ إضافة نقاط
ثم أرسل:
- آيدي المستخدم
- عدد النقاط
```

#### 2️⃣ خصم نقاط

```
لوحة المطور → ➖ خصم نقاط
```

#### 3️⃣ تغيير رسالة الترحيب

```
لوحة المطور → ✏️ تغيير الترحيب
```

#### 4️⃣ تغيير قناة الاشتراك

```
لوحة المطور → 📢 تغيير القناة
```

#### 5️⃣ عرض الإحصائيات

```
لوحة المطور → 📊 الإحصائيات
```

---

## 🔧 التخصيص المتقدم

### إضافة دول جديدة

في `config.py`:

```python
COUNTRIES = {
    "newcountry": {
        "name": "اسم الدولة",
        "flag": "🇽🇽",
        "code": "+123"
    }
}

PRICES["newcountry"] = 100  # السعر
```

### إضافة خدمات جديدة

```python
SERVICES = {
    "newservice": "📱 الخدمة الجديدة"
}
```

### تخصيص الرسائل

عدّل الرسائل في `config.py`:

```python
WELCOME_MESSAGE = """
رسالة الترحيب المخصصة
"""

PURCHASE_SUCCESS = """
رسالة نجاح الشراء
"""
```

---

## 🛠️ حل المشاكل الشائعة

### المشكلة: البوت لا يستجيب

**الحل:**
- تأكد من صحة التوكن في `config.py`
- تحقق من اتصالك بالإنترنت
- راجع ملف السجلات (logs)

### المشكلة: خطأ في الاشتراك الإجباري

**الحل:**
- تأكد أن البوت مسؤول في القناة
- تحقق من يوزر القناة في `config.py`
- استخدم @ قبل يوزر القناة

### المشكلة: لا تصل إشعارات المشتريات

**الحل:**
- اجعل البوت مسؤول في قناة المشتريات
- تأكد من يوزر القناة في `PURCHASES_CHANNEL`

### المشكلة: خطأ في تثبيت المكتبات

**الحل:**
```bash
pip install --upgrade pip
pip install -r requirements.txt --upgrade
```

---

## 📊 قاعدة البيانات

البوت يستخدم JSON لتخزين البيانات. الملفات في مجلد `data/`:

### users.json
```json
{
  "123456789": {
    "user_id": 123456789,
    "username": "user123",
    "first_name": "محمد",
    "points": 100,
    "purchases": 5,
    "join_date": "2024-01-01"
  }
}
```

### purchases.json
```json
{
  "123456789": [
    {
      "number": "+9647700000000",
      "service": "telegram",
      "country": "iraq",
      "price": 50,
      "date": "2024-01-01T12:00:00"
    }
  ]
}
```

---

## 🔐 الأمان والخصوصية

### نصائح الأمان:

1. **لا تشارك التوكن** - احتفظ بـ `config.py` سرياً
2. **قاعدة بيانات آمنة** - احفظ نسخة احتياطية من مجلد `data/`
3. **تحديثات منتظمة** - حدّث المكتبات دائماً
4. **مراجعة الكود** - افحص أي كود قبل إضافته

### النسخ الاحتياطي:

```bash
# نسخ احتياطي يومي
cp -r data/ backup/data_$(date +%Y%m%d)/
```

---

## 📈 التطوير المستقبلي

### مميزات قادمة (اختياري):

- ✅ دعم قواعد بيانات SQL (PostgreSQL/MySQL)
- ✅ نظام الإحالة المتقدم
- ✅ إحصائيات مفصلة ورسوم بيانية
- ✅ طرق دفع متعددة (Stripe, PayPal)
- ✅ لوحة تحكم ويب
- ✅ دعم اللغات المتعددة
- ✅ تطبيق موبايل

---

## 🤝 المساهمة

نرحب بأي مساهمات! يمكنك:

1. Fork المشروع
2. إنشاء فرع جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

---

## 📞 الدعم والتواصل

### هل تحتاج مساعدة؟

- 📧 **البريد الإلكتروني**: support@yourbot.com
- 💬 **تليجرام**: @YourSupport
- 📝 **التوثيق**: [docs.yourbot.com](https://docs.yourbot.com)

### الإبلاغ عن مشاكل

إذا وجدت مشكلة، افتح Issue في GitHub أو تواصل معنا مباشرة.

---

## 📜 الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](LICENSE) للتفاصيل.

---

## 🌟 شكر خاص

شكراً لاستخدامك هذا البوت! نتمنى أن يكون مفيداً لك.

إذا أعجبك المشروع، لا تنسَ:
- ⭐ النجمة في GitHub
- 🔄 المشاركة مع الأصدقاء
- 💬 ترك تعليق إيجابي

---

## 📝 ملاحظات إضافية

### للتشغيل على VPS:

```bash
# تثبيت screen
sudo apt install screen

# إنشاء جلسة جديدة
screen -S telegram-bot

# تشغيل البوت
python3 bot.py

# الخروج من screen (Ctrl + A ثم D)
# للرجوع: screen -r telegram-bot
```

### للتشغيل كخدمة Systemd:

أنشئ ملف `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Numbers Bot
After=network.target

[Service]
Type=simple
User=yourusername
WorkingDirectory=/path/to/telegram-numbers-bot
ExecStart=/usr/bin/python3 /path/to/telegram-numbers-bot/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

ثم:

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

---

## 🎯 نصائح للنجاح

1. **ابدأ بسيط** - جرّب البوت أولاً مع عدد قليل من المستخدمين
2. **استمع للمستخدمين** - خذ ملاحظاتهم وطوّر البوت
3. **احرص على الجودة** - أرقام سريعة وموثوقة = عملاء سعداء
4. **دعم ممتاز** - الرد السريع يبني الثقة
5. **أسعار منافسة** - ادرس السوق واختر الأفضل

---

## 📚 موارد إضافية

- [توثيق python-telegram-bot](https://docs.python-telegram-bot.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [دليل Python](https://docs.python.org/3/)

---

<div align="center">

**صُنع بـ ❤️ للمجتمع العربي**

⭐ **إذا أعجبك المشروع، لا تنسَ النجمة!** ⭐

</div>

</div>
